<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bbPatientApp.App"
                          xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
>
    <!--
        Define global resources and styles here, that apply to all pages in your app.
    -->

    <Application.Resources>
        
        <ResourceDictionary>
            <x:String x:Key="SFAPIKey">MTU1NjkyMUAzMjMxMmUzMTJlMzMzNWdHTDFRcEhoakJERElIVTFqMGFQVFV5K1BkRGpMV3VtSTFVZVl0N0RJQVU9</x:String>
            
            
            <Color x:Key="UOMPurple">#660099</Color>
            <Color x:Key="UOMGrey">#999999</Color>
            <Color x:Key="UOMYellow">#FFCC33</Color>

            <Color x:Key="BADBIRPurple">#4E2375</Color>
            <Color x:Key="Primary">#660099</Color>
            <Color x:Key="Secondary">#999999</Color>
            <Color x:Key="Danger">Maroon</Color>

            <Color x:Key="ButtonColourPrimary">#660099</Color>
            <Color x:Key="ButtonColourSecondary">#6c757d</Color>
            <Color x:Key="ButtonColourDanger">Maroon</Color>

            <Color x:Key="BADBIRPurpleBGLighter">#E0D0F1</Color>
            <Color x:Key="BADBIRPurpleBGAltLighter">#e1f1d0</Color>
            

            <Style TargetType="Label">
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
            </Style>

            <Style TargetType="ScrollView">
                <Setter Property="BackgroundColor" Value="White"/>
                <Setter Property="Padding" Value="5"/>
            </Style>
            <Style x:Key="MainOuterStackLayout" TargetType="StackLayout">
                <Setter Property="Padding" Value="10"/>
                <Setter Property="BackgroundColor" Value="White"/>

            </Style>


            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="CornerRadius" Value="5" />

                <Setter Property="TextColor" Value="White" />
                <Setter Property="BackgroundColor" Value="{StaticResource BADBIRPurple}" />
                <Setter Property="Margin" Value="15, 5" />
            </Style>

            <Style x:Key="PrimaryButtonPurple" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">

                <Setter Property="Margin" Value="5"/>

            </Style>


            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="BarTextColor" Value="White"/>
            </Style>

            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="Large" />
                <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
                <Setter Property="HorizontalTextAlignment" Value="Center"/>
            </Style>
            <Style x:Key="SubtitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="FontSize" Value="Medium"></Setter>
                <Setter Property="HorizontalOptions" Value="Start"/>
                <Setter Property="HorizontalTextAlignment" Value="Start"/>
                <Setter Property="Margin" Value="10"/>
            </Style>

            <Style x:Key="BodyLabel" TargetType="Label" BasedOn="{StaticResource SubtitleLabel}">
                <Setter Property="FontSize" Value="Small"/>

            </Style>

            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="PlaceholderColor" Value="LightGray"/>
                <Setter Property="BackgroundColor" Value="#ffe6ff" />
            </Style>
            <Style TargetType="DatePicker">
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="BackgroundColor" Value="#ffe6ff" />
                <Setter Property="FontSize" Value="Default"/>

            </Style>

            <Style TargetType="RadioButton">
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="BackgroundColor" Value="#ffe6ff"/>
            </Style>

            <Style  TargetType="Span">
                <Setter Property="TextColor" Value="{StaticResource BADBIRPurple}"/>
                <Setter Property="FontSize" Value="Small"/>

            </Style>

            <Style x:Key="DangerButton" TargetType="Button">
                <Setter Property="CornerRadius" Value="10" />

                <Setter Property="TextColor"
                    Value="White" />
                <Setter Property="BackgroundColor"
                    Value="{StaticResource ButtonColourDanger}" />
                <Setter Property="Margin"
                    Value="15, 5" />
            </Style>

            <Style x:Key="SecondaryButton" TargetType="Button">
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="BackgroundColor" Value="{StaticResource ButtonColourSecondary}" />
                <Setter Property="Margin" Value="15, 5" />
            </Style>

            <Style x:Key="QuestionSpacerV" TargetType="Line">
                <Setter Property="HeightRequest" Value="3"/>
            </Style>
            <Style x:Key="QuestionSeparatorV" TargetType="Line">
                <Setter Property="HeightRequest" Value="1"/>
                <Setter Property="IsVisible" Value="True"/>
                <Setter Property="BackgroundColor" Value="LightGray"/>
            </Style>

            <Style x:Key="HAQGrid" TargetType="Grid" >
                <Setter Property="VerticalOptions" Value="StartAndExpand"/>
                <Setter Property="Margin" Value="0,5"/>
                <Setter Property="xct:CornerRadiusEffect.CornerRadius" Value="5"/>
                <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
            </Style>
            <Style x:Key="HAQGridRowA" TargetType="Grid" BasedOn="{StaticResource HAQGrid}">
                <Setter Property="BackgroundColor" Value="{StaticResource BADBIRPurpleBGLighter}"/>
            </Style>
            <Style x:Key="HAQGridRowB" TargetType="Grid" BasedOn="{StaticResource HAQGrid}">
                <Setter Property="BackgroundColor" Value="{StaticResource BADBIRPurpleBGAltLighter}"/>
            </Style>
            
            
        </ResourceDictionary>
    </Application.Resources>
</Application>
